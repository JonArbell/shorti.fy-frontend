export interface MyUrlsResponse{

    id : number,
    originalUrl : string,
    shortUrl : string,
    currentClicked : number,
    maxClicked : number,
    expiryDate: string,
    isExpired : boolean

}